/** KO / EN 토글 — i18n 구조만 남긴 placeholder (동작 없음, 디자인 핸드오프 지침) */
export function LangToggle() {
  return (
    <button type="button" className="lang" aria-label="언어 전환 (준비 중)" disabled>
      KO <span>/</span> EN
    </button>
  );
}
